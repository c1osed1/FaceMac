

![Иконка FaceMac](Resources/Assets.xcassets/AppIcon.appiconset/icon_256x256.png)

# FaceMac

**Посмотри на Mac. Он откроется.**

Открытая разблокировка по лицу для macOS — бесплатно, приватно, живёт в нотче.
Без подписки. Без облака. Без аккаунта. Твоё лицо не покидает машину.

[English](README.md) · **Русский** · [中文](README.zh.md)



---

## Суть

Закрыл крышку, открыл — и снова вводишь пароль или прикладываешь палец. Двадцать раз в день. Зачем?

**FaceMac узнаёт тебя через встроенную камеру и вводит твой же сохранённый пароль**  
**на твоём же экране блокировки.** Нотч подсвечивается, сканирует, рисует галочку —  
и ты уже внутри, раньше, чем успел сесть. Около секунды, и камера гаснет.

## Почему переходят

- **Навсегда бесплатно.** Один бинарник, GPL-3.0, без триалов, ключей и «семейных тарифов».
- **Приватно по конструкции.** Кадр превращается в измерения **на твоём Mac**.
Кадры не пишутся на диск. Отпечатки лица и пароль не покидают машину.
- **Нотч — это интерфейс.** Настоящий оверлей: растёт из нотча, пульсирует зелёным
глифом лица, пока смотрит на тебя, и сворачивается в галочку.
- **Понимает, что это не ты.** Чужой получает отказ за ~0,4 с с красной тряской,
а не пятисекундное ожидание.
- **Камера выключается.** 5 секунд без совпадения — стоп. Зелёный индикатор горит
только пока она реально смотрит.
- **ML прямо на устройстве.** SFace (Apache-2.0) через CoreML, 128-мерные эмбеддинги,
калибровка под твоё лицо при регистрации.



## Как выглядит



### Нотч

![Анимация нотча FaceMac](docs/notch.gif)

Скан → совпадение → отторжение. Без окон и диалогов — всё в нотче.

### Настройки

![Настройки FaceMac](docs/settings.gif)

## Возможности


|                      |                                                                       |
| -------------------- | --------------------------------------------------------------------- |
| Оверлей нотча        | Настоящий `NSPanel` поверх всего, включая экран блокировки (SkyLight) |
| Анимация как Face ID | Зелёный скан → морф в галочку (Lottie)                                |
| Регистрация по шагам | Пять подсказок — прямо, влево, вправо, наклоны — с живым кольцом      |
| Отторжение           | «Явно не ты» ловится сразу и трясётся в красном                       |
| Калибровка           | Порог совпадения подстраивается под твоё лицо, камеру и свет          |
| Анти-дрейф           | Должны совпасть и лучший эталон, **и** центроид, N кадров подряд      |
| Отсев кадров         | Мелкие и сильно повёрнутые лица игнорируются, а не угадываются        |
| Кнопка повтора       | Стеклянная кнопка на локскрине, чтобы попробовать снова               |
| Языки                | English, Русский, 中文 — авто по системе или вручную                    |
| Приватность          | Всё локально, камера останавливается, когда закончила                 |




## Как это работает

```
кадр с камеры
  → детекция лица и 5 точек (Apple Vision)
  → similarity-выравнивание в кадр 112×112 (ArcFace)        (FaceAligner)
  → эмбеддинг SFace через CoreML, 128-d, L2-норма           (MLFaceEmbedder)
  → косинусная близость с регистрацией                      (FaceMatcher)
  → лучший + центроид + 3 кадра подряд должны совпасть
  → ввод пароля из Keychain через CGEvent                   (KeyboardInjector)
```

Никакого Python в рантайме, никакого сервера моделей, никаких сетевых вызовов
для распознавания.

## Установка

### Из релиза (проще всего)

1. Скачай `FaceMac-x.y.z.dmg` из **Releases**.
2. Открой и перетащи **FaceMac** в **Applications**.
3. Только при первом запуске: правый клик по приложению → **Открыть**. Сборки пока
   не нотаризованы, поэтому macOS спросит один раз — дальше всё работает.

### Из исходников

```sh
git clone https://github.com/<you>/FaceMac.git
cd FaceMac

scripts/fetch-model.sh   # соберёт CoreML-модель SFace (один раз, нужен Python 3.12+)
scripts/install.sh       # сборка, установка в ~/Applications и запуск
```

### Дальше из иконки в меню-баре

1. **Задать сохранённый пароль…** — пароль входа в macOS, в Keychain.
2. **Разрешить Accessibility…** — чтобы вводить пароль на локскрине.
3. **Зарегистрировать лицо…** — пять быстрых поз головы.
4. Оставь **Включено** и заблокируй Mac (`⌃⌘Q`).

**Показать анимацию нотча** в меню прогонит всё это без блокировки.

## Требования

- Mac на Apple silicon, macOS 14 или новее
- Встроенная камера (Continuity Camera не используется — iPhone не окажется рядом,
когда ты заперт снаружи)

Разрешения привязаны к **стабильной подписи кода**. FaceMac подписывается
идентити `Apple Development`, поэтому macOS сохраняет доступ к камере и
Accessibility между пересборками; ad-hoc подпись заставляла бы выдавать их каждый раз.

## Безопасность, честно

FaceMac — это **удобство**, а не апгрейд безопасности.

- Во встроенной камере нет ИК и датчика глубины. Печатная фотография не пройдёт,
а вот хорошее видео с тебя на экране телефона — может. Apple решает это железом;
вебка не может.
- Он хранит пароль входа в macOS в Keychain, потому что так он его вводит. Он не
покидает машину — но он **есть** на машине.
- Он не может запереть тебя снаружи: если он не сработал или ты его закрыл —
логинишься как обычно.
- Распознавание не запускается, пока экран разблокирован; камера выключена,
если скана нет.



## Сборка из исходников

```sh
swift build          # ядро + CLI
swift test           # 22 теста, включая паритет CoreML ↔ ONNX
xcodegen generate    # создать FaceMac.xcodeproj
```

Структура:

```
Sources/
  FaceMacCore/     камера, выравнивание Vision, SFace-эмбеддер, матчинг,
                   Keychain, слежение за блокировкой, ввод через CGEvent
  FaceMacApp/      меню-бар, оверлей нотча, окно настроек
  FaceMacDemo/     CLI без UI (info / enroll / match)
Tools/Conversion/  конвертация SFace ONNX → CoreML + golden-эмбеддинг
Tools/Icon/        генератор иконки приложения
scripts/           fetch-model.sh, install.sh, make-icon.sh
```



## Планы

- [x] Эмбеддинги SFace на устройстве + калибровка под лицо
- [x] Оверлей нотча поверх локскрина, анимация в стиле Face ID
- [x] Пошаговая регистрация, мгновенное отторжение, кнопка повтора
- [ ] Liveness (моргание / микродвижение)
- [ ] Несколько лиц на один Mac
- [ ] Поддержка pre-login (FileVault) через привилегированный хелпер
- [ ] Homebrew cask



## Благодарности

- **SFace** — OpenCV Zoo, Apache-2.0. Модель распознавания.
- **Atoll** — GPL-3.0. Подход к оверлею нотча.
- **SkyLightWindow** — MIT. Окно поверх экрана блокировки.
- **Lottie** — Apache-2.0.

Не связан с Apple. Face ID — торговая марка Apple Inc.

## Лицензия

GPL-3.0 — см. [LICENSE](LICENSE). Пользуйся, форкай, выпускай — только оставляй открытым.